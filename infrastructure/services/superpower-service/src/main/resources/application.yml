spring:
  application:
    name: @artifactId@

server:
  port: 24702

#Obs, using public ip won't work. Use DNS!
eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: @master.dns@/eureka

logging:
  level:
    se.omegapoint: DEBUG
