spring:
  application:
    name: "Employee Service"

server:
  port: 24701
  application:
    name: employee-service

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://54.194.242.215:8761/eureka/
  instance:
    preferIpAddress: true

logging:
  level:
    se.omegapoint: DEBUG
